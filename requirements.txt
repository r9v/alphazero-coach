# Game engine (installed separately — see Dockerfile or README)
# alphazero-boardgames @ git+https://github.com/r9v/alphazero-boardgames.git

# Backend
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
sse-starlette>=2.0
python-dotenv>=1.0

# LLM / Agent
langchain>=0.3
langchain-anthropic>=0.3
langchain-google-genai>=2.0
langgraph>=0.2

# Testing
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27  # for FastAPI TestClient async support

# Observability
langfuse>=2.0

# RAG — hybrid retrieval (dense + BM25), fusion, cross-encoder reranking
langchain-community>=0.3
chromadb>=0.5
sentence-transformers>=3.0   # bi-encoder embeddings + CrossEncoder reranker
rank-bm25>=0.2               # sparse lexical retrieval

# MCP server (exposes engine + strategy tools over Model Context Protocol)
mcp>=1.2
